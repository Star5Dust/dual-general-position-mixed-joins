# Research Rules

本项目中的所有 coding assistant 和 research assistant 必须遵守以下规则：

- 开始任何项目工作前，必须完整阅读根目录的 `PROJECT_STATUS.md`，并从其中记录的当前任务继续；不得仅凭聊天上下文猜测进度。
- 完成每个研究阶段后，必须更新 `PROJECT_STATUS.md` 的当前状态、已完成事项、`UNKNOWN` 项和唯一下一步，同时在 `notes/research_log.md` 写详细日志。

- 不得伪造数学定理、文献或引用。
- 不得把实验结果称为证明。
- 所有 conjecture（猜想）必须记录其产生依据。
- 找到反例时必须保留反例及其复现信息。
- 每个数学实现都必须有测试。
- 对关键结果尽量实现第二套独立验证方法。
- 数值结果必须可复现，并记录代码、参数和环境信息。
- 不确定时明确写 `UNKNOWN`。
- 不得为了迎合研究目标而隐藏失败结果、反例或不一致结果。
- 计算实验、猜想、证明和文献事实必须明确区分。
- 在系统文献检索完成前，不得声称当前 restricted problem 是新的或已经解决。
